#include<stdio.h>
int main(){
   int x,y;
   printf("enter the values of x:-");
   scanf("%d", &x);
   printf("enter the value of y:-");
   scanf("%d", &y);
   x=x+y;
   y=x-y;
   x=x-y;
   printf("After swaping the value are x=%d and y=%d",x,y);
   return 0;
}
